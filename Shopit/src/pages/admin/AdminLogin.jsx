import PagePlaceholder from '../../components/PagePlaceholder.jsx'

export default function AdminLogin() {
  return (
    <PagePlaceholder
      eyebrow="Admin"
      title="Admin login"
      description="Simple frontend-only admin credentials will be added when the admin section is built."
    />
  )
}
